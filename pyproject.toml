[project]
name = "project2"
version = "0.1.0"
description = ""
authors = [
    {name = "Sophie Brisson",email = "soph.brisson20@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "project2", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
